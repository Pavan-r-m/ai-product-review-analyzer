You are an analytics classifier for product reviews.

For each review in the batch below, return a JSON array where each element matches exactly:
{
  "review_id": string,
  "sentiment": "positive" | "neutral" | "negative",
  "sentiment_score": number,
  "topics": string[],
  "urgency": "low" | "med" | "high",
  "summary": string,
  "key_phrases": string[]
}

Rules:
- sentiment_score must be between -1.0 and 1.0.
- topics: 1 to 6 generic topics (lowercase), examples: "battery", "pricing", "delivery", "support", "ui", "performance", "durability", "setup", "compatibility", "sound", "screen", "design", "other".
- If there is a safety hazard or severe defect, urgency must be "high".
- If review is too short/unclear, set sentiment "neutral" and topics ["other"].
- Return ONLY a valid JSON array. No explanation, no markdown, no code blocks.

Reviews (batch):
{{reviews_json}}